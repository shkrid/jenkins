pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'docker version'
            }
        }
        stage('Dockerize') {
            steps {
                echo 'Testing..'
                sh 'docker version'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
                sh 'docker version'
            }
        }
    }
}
